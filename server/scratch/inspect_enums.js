const { GoogleAdsApi, enums } = require('google-ads-api');

console.log('AssetFieldType:', Object.keys(enums.AssetFieldType).slice(0, 10));
console.log('Example AssetFieldType.IMAGE value:', enums.AssetFieldType.IMAGE);
